# 新手纳戒与灵液状态修复

## 证据

- 抓包 `第一次创角色.json` idx 298/299：第一关 Boss 奖励 100200 ×3；第二关 Boss 奖励 101 ×1。
- idx 611、1639、5357：普通炼制 source=0，makeTimes=1/2/3，均为4颗 rid=1101、每颗280修为。
- idx 3359：独立 source=2 产物30修为，makeTimes仍为2。不能推导成“每第三次普通炼制出30修为”。
- PlayerLevelConfig：level=1/2/3 对应三/四/五段斗之气，升级消耗500、600；首轮1120修为最终剩20，与 idx 675 一致。
- 本地同一进程中，同一角色9月4日、5日两次初始化；第二次回到第一关，但丹药编号继续为8及9～12，证明旧临时状态未重置。

## 修改

- PlayerCreateHandler 注入同一个 SceneHandler，进入当前本地“从第一关重测”流程时同步清理该角色的临时战斗、任务、纳戒、丹药、等级和修为状态。数据库角色资料不删除；其他角色状态不清理。
- 普通炼制恢复为4颗×280，移除第三次出30修为的错误分支。
- 未服用丹药和纳戒激活技能按玩家隔离，避免相同丹药编号互相覆盖/消费。
- takingInfo 只记录实际服用的普通丹药，不按次数虚构 rid=1 的服用记录。
- 修正依赖错误炼制轮次的测试准备过程。

## 验证

- 修复前两项新用例失败：第三次仅出1颗；第二名玩家相同编号丹药无法服用。
- 修复后全量测试148项通过，无失败、错误或跳过。
- 新增初始化集成回归：同一角色两轮重测，首关纳戒奖励均为3，首份灵液突破到五段并剩20；其他玩家丹药、余额不受影响。
- 完成可执行包部署，HTTP18080与游戏19090监听，WebSocket本地握手通过。

## 边界

- 本次沿用已有“重进回第一关”的临时测试语义，不是角色进度持久化实现。
- source=2 的独立触发条件尚未确认，本次不凭时间或次数模拟。
- 现有灵液材料扣除仍使用抓包新手流程的背包槽位，尚不是完整的通用背包库存结算；本次未宣称解决任意领奖顺序的库存一致性。
- 客户端需退出后重新进入，实际界面验收尚待完成。
- 修复前源码与部署包：`D:\doudi-resources\backups\economy-fix-20260905-101238`。

## 后续启动修复

同日发现部署后的Java进程已退出，18080/19090均不监听，MySQL与ADB映射正常；日志及stderr没有记录Java异常，退出原因未能由日志定论。

`run-game-server.ps1` 改用 Windows WMI 创建隐藏独立进程并保留原有日志重定向、端口等待和重复启动检查。实测进程父链为 WmiPrvSE → cmd → java，启动命令返回后端口持续监听、WebSocket握手成功，重复运行脚本没有新增服务进程。启动脚本原件也保存在上述备份目录中。
