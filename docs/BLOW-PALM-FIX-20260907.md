# 吹火掌漏段/漏目标修复（2026-09-07）

范围：仅修复吹火掌 80130001 / 10150210100 的引导地图技能处理；不调整怪物属性，不改其他斗技的公式。

## 已修复

- 原来走 `writeBloodlineSlotHit`，即时对一个目标打一次，actionId=0。
- 现在按动画事件 433 / 767 / 1167 毫秒结算三段，actionId=10150210101/04/07，group=101/102/103，hitId=0/1/2。
- 每段从当前会话存活怪中选施法者周围半径10内最近最多7只；不打全场、不影响预载/其他会话。
- 保持施法位置，UseSkillResp 特效锚点为施法者位置，不再把服务端玩家位置移到目标脚下。
- 下发5秒冷却、costMp=100、activeSkill=true，并扣除100怒气（现有MP钳制机制）。
- 死亡动作和血量随伤害包下发；复用已有结算流程，避免在伤害特效播放前直接清掉单位。
- 2333毫秒下发空技能结束包；沿用会话身份检查，切波后不执行旧会话的技能回调。
- 吹火掌单段读取本波内已更新的 grantedAttack，而非 CombatUnit 入场时冻结的 attack。

## 证据

- `D:\抓包数据\第一次创角色.json`：idx8984施法位置，8986冷却；9115/9121/9123三段各6个目标，单段2331。8993/8999是另一场单段2124的样本，怪提前死亡。
- `D:\doudi-resources\client-analysis\config-json\6.8.1\Skill\Skills.json`：ID101502101，hit_0/1/2与end时序。
- 同目录 `Action.json`：10150210101/04/07，Radius=10，TargetCount=7，SelectSortType=7。
- 反编译 `Luban.Config.DamageTargetSortType`：7=Nearest。
- `skillfightconfig.json`：10150210100，Cooldown=5，CostValue=100。

## 明确未完成，不能宣称完全对齐官服数值

- 单段仍沿用现有攻击力取整结算，未把2124/2331硬编码，也没有通过单份抓包反推倍率。
- `skilldamageconfig.json` 提供主伤害率 `9000+50*star` 及固定附伤200、等级附伤20，但完整的属性修正、等级变量与最终伤害合并流程尚未复算确认。后续需独立实现并用多个属性/星级样本验证。
- 抓包施法时MP从100变0后还出现30；这份额外回怒来源未确认，本次不凭空补30。
- 本次修的是冷却回包；项目现有通用施法入口尚无完整的服务端冷却/怒气不足拒绝校验。本次未扩展该机制或猜测失败码。
- 抓包还有受击动作HitActionVO及相关控制效果；本次只补伤害/死亡/属性动作，没有实现完整受击硬直控制。
- 尚需真实客户端验收飘字、特效和实际清怪体验。通过服务端测试不等于完整画面验收。

## 验证与回退

- 新增3项回归：三段与属性更新/冷却；范围/最近7个/空目标与旧会话；死亡与单次结算/切波。
- 修改前新增测试失败（锚点错误、应7个实际1个）；修复后全量219项通过，无失败/跳过。
- `mvn -pl dpcq-game-server -am test -q`，部署打包也未跳过测试。
- 修改文件：SceneHandler.java、SceneHandlerTest.java，以及本说明。
- 修改前源码和JAR备份：`D:\doudi-resources\backups\blow-palm-20260907-155932`。
- 不修改参考工程 `D:\server\fuwu`，不重置数据库/角色存档。
