# 第二次抓包：第 15～17 关、任务、声望、天焚炼气塔

## 范围与结论

本次是在既有第 1～14 关实现上接续，不是重写服务端。沿用 Java 8、Spring/Netty、Player Actor 链路；`D:\server\fuwu` 仅作为只读架构参考，没有移植其玩法、数值。

已实现供本地联调的第 15～17 关三波小怪、Boss 战报回放、阶段切换、相关任务与奖励、声望 1→2 门槛，以及塔手动 1～3 层和托管到 19 层的进度。第 17 关后落在第 18 关第一波；不表示已实现第 18 关整关。

**这不是完整动态战斗或完整塔系统。** 新战斗沿用已有抓包战报回放，保留录制时的技能、伤害、时间轴和胜负；换装备后的实时伤害计算不在本次完成范围。尚未通过真实客户端完整走到第 17 关的验收。

## 证据

- 解码抓包：`D:\抓包数据\第二次抓包.json`，12,188,973 字节。
- SHA-256：`60A486191887E0324773D625823C73F751F039547E18BDC65F4635F6F569E620`。
- 对应原始 HAR：`C:\Users\ppp\Desktop\imlj2-cn-gamewx.lansors.com_2026_09_07_19_03_22.har`。二进制战报及协议样本从 HAR 提取，不将已解码 JSON 中的 64 位 ID 转成 JavaScript 浮点数再编码。
- 数值表：`D:\doudi-resources\client-analysis\config-json\6.8.1`。
- 客户端协议与调用源码：`D:\doudi-resources\client-analysis\decompiled-gameplay-runtime`。

| 功能 | 抓包索引/配置依据 | 本次处理 |
| --- | --- | --- |
| 第 15 关 | 战报 9473、9528、9583、9616 | 三波小怪、Boss、首通奖励；声望不足时留在第 15 关 |
| 第 16 关 | 9900、9928、9953、9989 | 声望 2 解锁，三波与 Boss、任务与奖励 |
| 第 17 关 | 10013、10033、10081、10264 | 三波与 Boss、任务与奖励，进入第 18 关第一波 |
| 任务 200035 | 9561；taskconfig | 实际通塔 2 层才完成，移除自动送完成 |
| 任务 200036～200038 | taskconfig；9715～9805 的炼化链 | Boss14、角色 13 级、Boss15 条件分别校验 |
| 声望 1→2 | 9883～9889；reputationconfig | 消耗 700 声望道具，开启 200100、200101、200111 |
| 后续主线任务 | taskconfig、10136～10285 | 抵达16、收挂机装备、Boss16、首次通用技能抽取、Boss17 |
| 塔手动 | 战报 9500、9546、9567；powertowerconfig | 1～3 层；前两层结算与任务完成可交叉核对 |
| 塔托管 | 9576～10356 | 按录制间隔递增到19层，结束原因1；不虚构奖励结算 |

任务 200030、200033、200034、200035 各给 100 声望道具，200037 给 200，200038 给 100，共 700；200036 奖励普通灵液 102×5。新任务通过条件检查与一次性领奖集合防止重复领取。

## 关键实现

- `SceneHandler.java`：阶段推进、任务条件、声望门槛、普通灵液消耗、首次通用技能引导抽取、塔请求与有限托管调度。
- `ChapterConfig.java`：新增关卡与战报中真实怪物模板、数量、属性；不沿用第 15 关占位怪物。
- `PlayerRealmConfig.java`：从 Luban 补角色 12～17 级属性。
- `Chapter17Data.java`、`Chapter17Progress.java`：本次所需配置与 Actor 内存状态。
- `PlayerConnectionContext.java`：跟踪已经下发的货币余额及普通背包槽，新奖励按已有余额累加并复用物品槽。不是全服持久化背包重构。
- `WutanBattleLogIdentity.java`：新协议对象引用映射；补 `HitActionVO.attackId/targetId`，防止击退事件指向录制角色。
- `WutanCapturedLog.java`：受击事件起点、终点随本地战斗坐标平移。
- `scene.proto`、`chapter17.proto`：字段号以客户端 ProtoMember 为准，保留现有协议兼容性。
- `tools/extract_wutan_bins.js`：支持指定关卡及塔战报导出。
- `tools/export_ch17_support.js`：导出所需客户端消息、Luban 配置、少量二进制协议样本。只读本地源资料。

## 明确未完成

1. **托管奖励领取未完成。** 抓包只有托管前 `FireTowerHostResultResp.hasResult=false`，没有托管后的成功结算及背包、货币变化。当前只推进已录制的层数，不发猜测的奖励；查询时记录 `UNRESOLVED tower host reward settlement`。建议先验收手动前两层任务链，暂不将托管当成可领取收益功能。
2. **手动第 4 层及以后未接通。** 没有对应手动战报，也没有完整动态战斗；当前不生成假的挑战成功包。塔界面本地支持上限暂为19，不代表官服的每日/总层数上限。
3. 塔计时收益、礼包领取、排行、跨天重置未实现。礼包信息不冒用官服人数/名次。
4. 通用技能抽取只覆盖本次抓包的首次引导抽取及自动激活，不是完整随机奖池或所有升星操作。
5. 本次进度沿用项目已有**内存状态**。服务重启不保证任务、塔、关卡状态恢复；没有新增数据库持久化，也没有清库或修改账号存档。不能据此宣称生产可用或断线重启完整恢复。
6. 第 17 关之后的后续任务链（除已存在的声望任务）与第 18 关整关不在本次范围。

需要补充：托管结束后点击领取的完整请求/响应与背包、货币变化；手动第4层起战斗记录；塔计时奖励/礼包领取记录。若要求任意角色属性都能正确战斗，需要另行实现可验证的动态战斗结算，不能靠复用固定战报完成。

## 验证与联调

新增测试覆盖：真实塔条件、5份灵液实际炼化服药达到13级、700声望升阶、16/17关任务链、奖励累加与槽位复用、重复领奖/结算拦截、非法击杀不结算、托管取消旧回调失效、未虚构托管奖励、11～17关对象ID全面替换、受击位移和非坐标字段保持。

执行全量测试：

```powershell
cd D:\doudi-resources\doupo-server
. .\dev-env.ps1
mvn -pl dpcq-game-server -am test -q
```

客户端验收顺序：先完成解锁塔的主线任务 → 通塔1、2层并领取200035 → Boss14后领取200036 → 用5份普通灵液炼化/服药达到13级领取200037 → Boss15领取200038 → 声望升2并进入16 → 收取两件挂机装备、Boss16、通用技能引导抽取 → Boss17与200104奖励。服务端测试通过不能替代这一遍客户端验收。

修改前旧 JAR 及主要原文件备份：`D:\doudi-resources\backups\ch17-tower-20260908`。未修改参考工程、原抓包、Luban 原表和客户端文件。

## 本轮验证与部署结果

- 2026-09-08：`mvn -pl dpcq-game-server -am test -q` 与 `package -q` 均成功；257项测试，0失败、0错误、0跳过，没有使用 `-DskipTests`。
- 回归曾发现 `HitActionVO` 未定义导致录制角色ID残留；补字段与坐标映射后整段字节流检查通过。位移测试使用0.001浮点容差，非坐标字段仍逐值核对。
- 17:53启动本轮 JAR，服务PID **11444**，18080/19090由同一进程监听，`/health` 返回 `UP`。
- 模拟器 `emulator-5554` 的 ADB reverse：18080→18080、19090→19090已配置。
- JAR SHA-256：`4E2143C4F83A793F10136CA05545A50F9646842993B4B68B58826B732AC1ACC6`，75,552,657字节。
- 启动日志：`D:\doudi-resources\doupo-server\dpcq-game-server\target\run-logs\server-20260908-175324.out.log`；对应 `.err.log` 为空。
- 已验证构建、单测和服务启动；**真实客户端完整任务/战斗通关尚未验收**。发现问题应提供发生时间、当前任务/关卡和客户端表现，再对照这份日志定位。
- 两个本次误导出的无用原始样本 `9498-50366.bin`、`9544-60751.bin` 已从源码资源目录移入备份下 `unused-generated-fixtures`，可恢复；没有删除用户抓包或存档。
