# 第9关本地测试入口

只用于本地快速回归。普通启动不改变新手流程；不改 APK，不改官方服。

## 启动

先停止本项目正在运行的游戏服务（不要停 MySQL 或其他 Java 进程）。
确认接受当前内存进度丢失后，在项目根目录执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\run-game-server.ps1 -Chapter9Test
```

启动日志显示 `Chapter 9 TEST mode`；重新登录、创建角色，日志出现
`Chapter 9 TEST checkpoint initialized` 后直接进入 `10200401`（第9关第一波）。
此模式每次角色初始化都重置到该检查点，不是存档/断线续玩功能。
不自动停止现有服务；如果现有进程不是第9关模式，脚本会提示先停止。

恢复第1关测试：停止游戏服务，去掉 `-Chapter9Test` 再启动。默认配置不启用此开关。
Java 参数等价于 `--game.test.chapter9-start=true`，仅应在本地测试环境使用。

## 数据依据及恢复范围

来源：`D:\抓包数据\第一次创角色.json`，截至 idx 7328，下一条 7329 为进入第9关。
SHA-256：`5D45451945E4064A7F995684C6A7FDB5BA9E40987D1F2095763A951BAA0AF55A`。
这个检查点不是整账号存档；它恢复第9关战斗回归所需状态，不声称恢复所有活动、货币、邮件。

| 状态 | 恢复内容 | 依据 |
| --- | --- | --- |
| 关卡 | 10200401，历史最高10200305，Boss未解锁 | 7322、7329 |
| 境界 | 内部level=7、stage=1，即九段斗之气 | 6709、6718及playerlevelconfig |
| 攻击/最大血量/防御 | 1846.54656 / 47938.8 / 728 | HeroStatUpdate、7177、7311、7330 |
| 战力 | 25778 | 6965、6966 |
| 修为/灵液 | 修为10、筑基库存及成品为空、炼制4次、已服用16份 | 6645–6710，累计背包更新 |
| 纳戒 | 能量29、累计12抽、阶段2（阶段计数5/7） | 6945、7324 |
| 已学斗技 | 80140001、80130011、80130101、80128011、100001、80128001，均2星 | 333–6960的激活/升星更新 |
| 装配 | 2001→80128011、1001→100001，3001已解锁但为空 | 5859、6717 |
| 主线 | 200023进行中；抽取、领奖后正常接200024 | 7327、7328 |
| 引导 | 只恢复7329之前已保存的15组引导 | 50366累计结果 |

抓包里80128001在idx6465已经获得，但尚未装备，不能据旧代码注释认为它只在第9关之后获得。
药老临时技能101001没有提前授予；10043教学没有标记完成。
三波小怪、Boss解锁、濒死教学及后续第10关仍走现有处理链。

## 验证

新增 `Chapter9TestCheckpointTest` 覆盖：默认第1关、检查点初始化、29点能量抽后剩9、
主线完成、重复创建不叠加物品、场景与服务端Boss会话数值一致、教学未提前触发。
保留并运行 `NinthBossGuideTimingTest` 验证之前修复的重复濒死问题。
客户端最终表现仍需实机测试，不能用协议单测替代。

当前部署状态：按用户要求，已于2026-09-09 14:44关闭第9关测试开关并重启，恢复第1关普通测试。
运行包及修复代码不变；新PID15672不带`--game.test.chapter9-start=true`，18080/19090及健康检查正常。
MySQL PID2180未重启、数据库未清空。游戏内存进度已清空，重新登录按普通新手流程初始化。
当前启动日志：`dpcq-game-server/target/run-logs/server-20260909-144403.out.log`。

以下为14:39启用第9关模式时的部署记录（该模式现已关闭）：

2026-09-09 构建验证：318项测试通过，失败/错误/跳过均为0，启动脚本语法检查通过。
隔离构建包：`D:\doudi-resources\build-staging\chapter9-start-20260909\dpcq-game-server\target\dpcq-game-server-0.0.1-SNAPSHOT.jar`。
包SHA-256：`D6CFCC8458021E819FC9A6025D6763C51DE1F1AE8099C60C65F4FE6A87EED998`。
该包同时包含此前的第9关重复濒死时序修复。旧PID19700已停止，新PID2856的命令行已确认包含
`--game.test.chapter9-start=true`。18080/19090监听正常，`/health`返回UP，模拟器两端口反向转发正常。
启动时MySQL未监听3307，现有启动脚本已将其启动（PID2180），未清空数据库。
旧包备份：`D:\doudi-resources\backups\chapter9-test-start-20260909\before-start.jar`。
当前内存进度已随旧服务退出清空，等待客户端重新登录验证第9关实机表现。
启动日志：`dpcq-game-server/target/run-logs/server-20260909-143953.out.log`。
